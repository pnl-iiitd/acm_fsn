apiVersion: v1
kind: Pod
metadata:
  name: client-pod
spec:
  restartPolicy: Never
  containers:
  - name: test-client
    image: nginx
    command: ["/bin/sh"]
    args: ["-c", "curl <insert IP here>:80/"]
